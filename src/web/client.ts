// fleetview browser client — opens a WebSocket to the server and live-renders
// the fleet dashboard. Filled in from T9 onward; this stub keeps the web build
// honest (browser-loadable ESM, no bundler, native WebSocket).
export const CLIENT_VERSION = "0.1.0";
